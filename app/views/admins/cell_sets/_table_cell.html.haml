.row-fluid(id="availableContentCell_#{cell.id}" class="contentCell availableCell")
  .contentCellPosterImage.thumbnail.span2
    = image_tag Cell.dummy_poster_image
    -#TODO should be the actual poster_image, but this would lead to too many requests
    -#     instead we should filter the available sets by type or something like that.
    -#     @see Case 26630
    -#- if cell.poster_image
    -#  = image_tag cell.poster_image.url
  .span8
    %h5.contentCellTitle= cell.title
    %p.contentCellDescription= cell.description
  .span2.contentCellShowButton= t('set.tabs.show_info')
